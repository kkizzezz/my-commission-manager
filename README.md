# 🎨 Commission Manager (Bakery Theme)

ระบบจัดการคิวงานวาดรูปและคำนวณราคา สไตล์ Warm Minimalist Bakery & Cafe 🥐✨
พัฒนาด้วย **React + Vite + Tailwind CSS** โดยเชื่อมต่อกับ **Google Sheets** เพื่อซิงค์ข้อมูลข้ามอุปกรณ์

## ✨ ฟีเจอร์หลัก (Key Features)
- **Dashboard:** คำนวณราคาอัตโนมัติ (บวกเพิ่มเชิงพาณิชย์, ไฟล์ Ai, ฯลฯ)
- **Queue Management:** จัดการสถานะงาน (รอชำระมัดจำ -> บรีฟ -> ร่าง -> ตัดเส้น -> ลงสี -> เสร็จ)
- **Receipt Generator:** สร้างใบเสนอราคา/ใบเสร็จ สไตล์กระดาษใบเสร็จจริง พร้อมบันทึกเป็นรูปภาพ
- **Cross-Device Sync:** เชื่อมต่อกับ Google Sheets ข้ามเครื่อง ข้ามมือถือ ข้อมูลไม่หาย
- **Auto-Sync:** บันทึกข้อมูลลง Google Sheets ทันทีเมื่อมีการเปลี่ยนแปลง

---

### ⚠️ วิธีแก้ปัญหา "หน้าขาว" หรือ "เห็นโค้ดภาษาอังกฤษ" (White Screen Fix)
หากคุณเปิดลิงก์แล้วเจอหน้าขาว หรือเห็นโค้ดภาษาอังกฤษ (เช่น `import ...`) แสดงว่าคุณตั้งค่า GitHub Pages ผิดวิธีครับ **ห้ามเลือก "Deploy from a branch"**

**วิธีแก้ไขที่ถูกต้อง:**
1. ไปที่เมนู **Settings** > **Pages**
2. ในหัวข้อ **Build and deployment** > **Source** ให้เปลี่ยนจาก "Deploy from a branch" เป็น **"GitHub Actions"** เท่านั้น!
3. จากนั้นไปที่แถบ **Actions** ด้านบน แล้วรอให้กระบวนการ "Deploy to GitHub Pages" ขึ้นเครื่องหมายถูกสีเขียว ✅ ทั้งหมด
4. เมื่อเสร็จแล้ว ลิงก์เดิมจะใช้งานได้ทันทีครับ

---

## 🚀 วิธีนำขึ้น GitHub (Deployment)

โปรเจกต์นี้ตั้งค่า **GitHub Actions** ไว้แล้ว เมื่อคุณอัปโหลดโค้ดขึ้น GitHub ระบบจะสร้างหน้าเว็บให้ทันที

### ขั้นตอนการอัปโหลด:
1. **สร้าง Repository ใหม่** ใน GitHub ของคุณ
2. **อัปโหลดไฟล์ทั้งหมด** ในโฟลเดอร์นี้ขึ้นไป (ยกเว้น `node_modules` และ `dist`)
3. ไปที่เมนู **Settings** ของ Repository
4. เลือกเมนู **Pages** (ด้านซ้าย)
5. ในหัวข้อ **Build and deployment** > **Source** ให้เลือกเป็น **GitHub Actions** (ห้ามเลือกอย่างอื่น)
6. รอสักครู่ ระบบจะสร้างลิงก์เว็บไซต์ให้คุณใช้งานได้ทันที! (เช่น `https://yourname.github.io/repo-name/`)

---

## 📊 การตั้งค่า Google Sheets (Database Setup)

เพื่อให้ข้อมูลซิงค์ได้ คุณต้องทำตามขั้นตอนดังนี้ (ถ้าทำแล้วข้ามได้เลย):

1. **สร้าง Google Sheet:** สร้างไฟล์ใหม่ ตั้งชื่อชีทแรกว่า `Queue` และชีทที่สองว่า `Archive`
2. **หัวตาราง:** ในทั้ง 2 ชีท ให้พิมพ์ `id` ในช่อง A1 และ `data` ในช่อง B1
3. **Apps Script:** ไปที่เมนู `Extensions` > `Apps Script` แล้ววางโค้ด API (ดูในไฟล์ `src/App.tsx` หรือขอจากผู้ช่วย)
4. **Deploy:** กด Deploy > New Deployment > Web App (เลือก Who has access: Anyone)
5. **URL:** นำ Web App URL ที่ได้ไปวางใน `src/App.tsx` ตรงตัวแปร `GOOGLE_SCRIPT_URL`

---

## 🛠️ การติดตั้งในเครื่อง (Local Development)

หากต้องการแก้ไขโค้ดในเครื่อง:

1. ติดตั้ง Dependencies:
   ```bash
   npm install
   ```

2. รันเซิร์ฟเวอร์ทดสอบ:
   ```bash
   npm run dev
   ```

3. สร้างไฟล์สำหรับใช้งานจริง (Build):
   ```bash
   npm run build
   ```
   *จะได้ไฟล์ `dist/index.html` ที่สามารถเปิดใช้งานได้เลย*
